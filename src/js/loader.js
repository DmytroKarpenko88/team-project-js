const loader = document.querySelector('.loader');

window.addEventListener('load', () => {
  // loader.classList.add('done');
});
if (loader) {
  window.addEventListener('load', () => {
    loader.classList.add('done');
  });
}
